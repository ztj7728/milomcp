# MCP Server Configuration Example
# Copy this file to .env and fill in your actual values.

# -- Server Settings --
# The port the HTTP server will listen on.
PORT=3000

# The port the WebSocket server will listen on.
WS_PORT=3001

# -- Tools Settings --
# The directory where your tool files are located.
TOOLS_DIR=./tools

# -- Authentication Settings --
# Set to "false" to disable authentication and rate limiting.
AUTH_ENABLED=true

# A secret key for signing JWT tokens. Use a long, random string.
# You can generate one here: https://djecrety.ir/
JWT_SECRET=your-super-secret-and-long-string-for-jwt

# The master administrator token. This token has unlimited access.
# Keep this token secure and private.
ADMIN_TOKEN=your-super-secret-admin-token

# -- Rate Limiting --
# Set to "false" to disable rate limiting for all tokens.
RATE_LIMITING_ENABLED=true

# -- Tool Specific API Keys --
# API Key for the weather tool (Amap Web Service)
# Get one from https://console.amap.com/
WEATHER_API_KEY=your_amap_weather_api_key