# MCP Server Configuration Example
# Copy this file to .env and fill in your actual values.

# -- Server Settings --
# The port the HTTP server will listen on.
PORT=3000

# The directory where your tool files are located.
TOOLS_DIR=./tools

# -- Authentication Settings --
# Secret key for signing JWT Access Tokens
JWT_SECRET=replace_with_a_long_random_string

# Initial Admin Credentials (used only on first startup if no admin exists)
INITIAL_ADMIN_USER=admin
INITIAL_ADMIN_PASSWORD=replace_with_a_strong_secret_password

# A secret key for encrypting/decrypting user environment variables
# Should be 32 characters long for AES-256
ENCRYPTION_KEY=replace_with_a_32_character_long_secret_key

# -- Tool Specific API Keys --
# API Key for the weather tool (Amap Web Service)
# Get one from https://console.amap.com/
WEATHER_API_KEY=your_amap_weather_api_key
